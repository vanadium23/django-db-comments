============
Installation
============

At the command line::

    $ easy_install django-db-comments

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-db-comments
    $ pip install django-db-comments
